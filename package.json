{
  "name": "dragon-clan-simulator",
  "version": "0.1.0",
  "description": "A 2D simulation game of a clan of dragons",
  "type": "module",
  "scripts": {
    "build:wasm": "cd rust && wasm-pack build --target web",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist rust/pkg"
  },
  "keywords": ["game", "simulation", "dragons"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}

